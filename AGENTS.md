# AGENTS.md

このリポジトリでCodexが作業する際の共通ルールです。

## 基本方針
- 回答・説明は日本語で行うこと。
- 大きな仕様変更を独断で決めないこと。必ず候補案と、それぞれのメリット・デメリットを提示して合意を取ること。
- 小さな単位でPRを作成すること（1PR 1目的を意識する）。
- Unityの自動生成ファイル（ProjectSettings配下や`.meta`、自動更新されるアセット設定など）を不要に編集しないこと。

## ステージデータ運用
- ステージデータ仕様を変更する場合は、`docs/stage_data_design.md` を必ず同時更新すること。
- JSONのキー命名・構造は設計書に合わせ、差分が発生した場合は変更理由を明記すること。

## 作業完了時
- 実装後は以下を必ずまとめて報告すること。
  - 変更ファイル一覧
  - 各ファイルで何を変更したか
  - 確認方法（実行コマンド、Unity上での確認手順など）
